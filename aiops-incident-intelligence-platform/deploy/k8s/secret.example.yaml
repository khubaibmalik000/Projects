# Template only -- do not commit a filled-in copy. Create the real secret with:
#   kubectl create secret generic aiops-db-credentials \
#     --from-literal=database-url='postgresql+psycopg2://aiops:CHANGE_ME@postgres:5432/aiops'
apiVersion: v1
kind: Secret
metadata:
  name: aiops-db-credentials
type: Opaque
stringData:
  database-url: "postgresql+psycopg2://aiops:CHANGE_ME@postgres:5432/aiops"
