---
galaxy_info:
  role_name: nginx
  description: >
    Installs and configures nginx. Converted from the legacy nginx Chef
    cookbook as the pilot conversion used to validate the whole
    Chef-to-Ansible process before rolling it out to the rest of the estate.
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions: [8, 9]

dependencies: []
